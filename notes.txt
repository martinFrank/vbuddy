So führst du die Tests aus

  # Normales mvn test — LLM-Tests werden NICHT ausgeführt
  mvn test                                                                                                                 
  # NUR die LLM-Tests ausführen (Ollama muss laufen)                                                                       mvn test -Dgroups=llm -DexcludedGroups=
                                                                                                                           # Einzelne Testklasse
  mvn test -Dgroups=llm -DexcludedGroups= -Dtest=PlanningAiServiceLlmTest   