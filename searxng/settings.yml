use_default_settings: true

general:
  debug: false
  instance_name: "VBuddy Search"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "de"
  formats:
    - html
    - json

server:
  secret_key: "vbuddy-searxng-secret-key-dev"
  bind_address: "0.0.0.0"
  port: 8080
